{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "pnpm --filter @workspace/csa-2026 dev",
    "dev:api": "pnpm --filter @workspace/api-server dev",
    "dev:all": "pnpm -r --parallel run dev --filter @workspace/api-server --filter @workspace/csa-2026",
    "preinstall": "node -e \"const u=process.env.npm_config_user_agent||''; if(!/^pnpm\\//.test(u)) { console.error('Use pnpm instead'); process.exit(1); }\"",
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck"
  },
  "private": true,
  "devDependencies": {
    "@rollup/rollup-win32-x64-msvc": "^4.59.0",
    "prettier": "^3.8.1",
    "typescript": "~5.9.2"
  }
}
